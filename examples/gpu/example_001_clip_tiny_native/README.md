# GPU Example 001: Tiny CLIP Native

Run from the repository root:

```bash
bash examples/gpu/example_001_clip_tiny_native/run.sh
```

This example uses the unified runtime with CUDA/NCCL hints and the native backend.
