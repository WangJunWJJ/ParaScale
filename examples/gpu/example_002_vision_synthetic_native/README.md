# GPU Example 002: Vision Synthetic Native

Run from the repository root:

```bash
bash examples/gpu/example_002_vision_synthetic_native/run.sh
```

This example uses the unified runtime with CUDA/NCCL hints and a tiny synthetic
vision workload.
