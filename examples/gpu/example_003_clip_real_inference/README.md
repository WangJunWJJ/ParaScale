# GPU Real CLIP Inference

Runs ParaScale inference with a real CLIP checkpoint on CUDA.

```bash
bash examples/gpu/example_003_clip_real_inference/run.sh
```

Expected model mount:

```text
/models/clip/openai_clip-vit-base-patch32
```
