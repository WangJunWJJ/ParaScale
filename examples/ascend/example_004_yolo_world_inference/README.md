# Ascend Example 004: YOLO-World Inference

Run from the repository root inside an Ascend torch_npu environment:

```bash
bash examples/ascend/example_004_yolo_world_inference/run.sh
```

This example validates the generic inference runtime on NPU with a YOLO-world
style vision detection adapter.
