# Ascend Real YOLO-World Inference

Runs ParaScale inference with a real Ultralytics YOLO-World checkpoint.

```bash
bash examples/ascend/example_006_yolo_world_real_inference/run.sh
```

Expected model mount:

```text
/models/yolo/yolov8s-worldv2.pt
```
