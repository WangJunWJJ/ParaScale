# Ascend Real CLIP Inference

Runs ParaScale inference with a real Hugging Face CLIP checkpoint on Ascend NPU.

```bash
bash examples/ascend/example_005_clip_real_inference/run.sh
```

Expected model mount:

```text
/models/clip/openai-mirror/clip-vit-base-patch32
```
