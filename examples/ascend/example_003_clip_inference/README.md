# Ascend Example 003: CLIP Inference

Run from the repository root inside an Ascend torch_npu environment:

```bash
bash examples/ascend/example_003_clip_inference/run.sh
```

This example validates the generic inference runtime on NPU with a CLIP-style
multimodal embedding adapter.
