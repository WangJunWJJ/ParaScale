# Ascend Example 001: Tiny Ascend Native

Run from the repository root inside an Ascend torch_npu environment:

```bash
bash examples/ascend/example_001_tiny_ascend_native/run.sh
```

This example uses the unified runtime with NPU/HCCL hints and the `ascend_native`
backend for a single NPU smoke.
